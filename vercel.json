{
  "buildCommand": "cd project && npm run build",
  "outputDirectory": "project/dist",
  "framework": "vite",
  "functions": {
    "api/**": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
